A bunch of scripts to fix issues encountered with VMWare Workstation on Linux (Ubuntu).
May need modification to suit your purposes.

- Workaround for "GNU C Compiler not found" issue.
sudo ./recompile-modules.bash
sudo ./vmkernel-prep.bash
./run-vmware.bash
